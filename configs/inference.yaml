# Inference & post-processing. `thresholds` and `median_filter_seconds` are
# MODEL-SPECIFIC, auto-calibrated by scripts/06_tune_thresholds.py --write
# (re-run after every retrain; it rewrites this file without comments).
# Calibrated for weights/panns_pcen.pt (epoch 37): synth-val IER 0.5677.
block_seconds: 30.0
block_overlap_seconds: 2.0
posteriors:
  median_filter_seconds: 1.2
thresholds:
  vessel:
    high: 0.5
    low: 0.2
  marine_animal:
    high: 0.15
    low: 0.09
  natural_sound:
    high: 0.15
    low: 0.06
  other_anthropogenic:
    high: 0.15
    low: 0.06
events:
  merge_gap_seconds: 1.0
  min_duration_seconds: 1.0
  round_to_seconds: true
  enforce_non_overlap: true
json:
  startup_name: ECHOSENTINEL
