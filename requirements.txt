# Streamlit Community Cloud dependencies for the echoSentinel console.
# CPU-only torch (Community Cloud has no GPU); the CPU index keeps the image small.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.36
torch>=2.2
torchaudio>=2.2
numpy>=1.24,<2.1
scipy>=1.10
pandas>=2.0,<2.4
librosa>=0.10.2
soundfile>=0.12.1
soxr>=0.3.7
omegaconf>=2.3.0
pillow>=10.0
altair>=5.0
gdown>=5.1
